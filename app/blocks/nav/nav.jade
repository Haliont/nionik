mixin nav(items, withActive)
	+b.UL.nav&attributes(attributes)
		- if (withActive)
			+e.LI.item--active
				+e.A.link(href=jv0)= items[0]
		- for item in (withActive ? items.slice(1) : items)
			+e.LI.item
				+e.A.link(href=jv0)= item
