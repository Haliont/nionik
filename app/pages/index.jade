extends ../layouts/main

include ../blocks/hero/hero
include ../blocks/app-promo/app-promo

block head
	- var pageTitle = 'nionik — детские часы с GPS-трекером';

block content
	+hero()
	+app-promo()
